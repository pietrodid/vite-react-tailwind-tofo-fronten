const Footer = () => {
   return (
      <section>
         <footer className="mt-8 text-center transition-all duration-1000 dark:text-gray-300">
            Drag and drop to
         </footer>
      </section>
   );
};

export default Footer;
